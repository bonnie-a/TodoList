<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todolist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UpdateActivity"
            android:label="@string/title_activity_update_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".AddTaskActivity"
            android:label="@string/title_activity_add_task"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TaskActivity"
            android:label="@string/title_activity_task"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UpdateTodoList"
            android:label="@string/title_activity_update_todo_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddTodoListActivity"
            android:label="@string/title_activity_add_todo_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TodoListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>